---
"@slicemedia/swiper-adapter": minor
---

Add opt-in attribute-based slider structure preparation for existing Webflow grids and CMS lists. Resolve scoped tracks, slides, and containers; temporarily apply upstream classes and flex mechanics; restore authored layout and list semantics on teardown. Handle delayed or replaced tracks, new keyed CMS slides, nested controls, container ownership and measurement, failed initialization, and vendor mutation feedback. Keep standard markup and inert imports compatible.
